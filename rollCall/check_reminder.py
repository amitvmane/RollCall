from exceptions import *
from datetime import datetime
import pytz

async def check(rollCalls):
    while condition==True:
        for rollCall in rollCalls:
            if rollCall.finalize_date!=None and rollCall.finalize_date==datetime.utcnow().strftime("%d-%m-%Y %H:%M"):
                if rollCall.finalize_date:
                    pass